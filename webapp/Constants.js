sap.ui.define([],
function() {
    "use strict";

    var constants = {
        BASE_URL: "http://localhost:8083",
        LOGIN_PATH: "/loginUser",
        USERS_PATH: "/users",
        PROGRAMS_PATH: "/programs"
    };

    return constants;
});